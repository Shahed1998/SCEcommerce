@model NotificationViewModel

@if (Model != null && Model.showtoastMessage == true)
{
    <script>
        ToastrNotification('@Model.NotificationStatus', '@Model.NotificationMessage');
    </script>
}
