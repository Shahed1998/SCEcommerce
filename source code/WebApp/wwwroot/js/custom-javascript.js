function CreateModal(e, el) {
    e.preventDefault();
    const href = $(el).attr('href');
    //console.log(href);
    // add ajax call, so that the modal is loaded dynamically and can be called from anywhere

}